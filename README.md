# renderMsg Utility

Utilities for generating safe HTML snippets summarizing rompimento (breakout) checks across manual/automático modes. Includes a Node test suite covering strict/inclusive rules and directional logic.

## Scripts

- `npm test` – run the Node.js test runner against the suite in `tests/`.
